// routes that render their own header/footer (or none at all) and
// shouldn't get the marketing site's Navbar/Footer wrapped around them
export const HIDDEN_CHROME_ROUTES = ["/signup", "/login", "/forgot-password", "/accept-invite", "/demo", "/dashboard", "/admin"];
